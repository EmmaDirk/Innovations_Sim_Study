# use this script to load any package needed 
# -----------------------------------------------------------------

# required packages
pkgs <- c("tidyverse")

# load packages
lapply(pkgs, library, character.only = TRUE)